# Music Files

Place Qing-UU's `.mp3` or `.flac` files here for the music player.

The playlist in `components/interactive/MusicPlayer.tsx` references:

- `からっぽの星で.mp3`
- `走り出した朝に.mp3`

Replace these files or update the playlist array when real music assets are ready.

> Audio files are excluded from git via `.gitignore`.
